# Test Package

This is a sample project that *only* tests the correctness *package*:
it checks if the *conan* package can be properly consumed by clients.

This is *not* related to testing the code or any requirements of the library itself.
